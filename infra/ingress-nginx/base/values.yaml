ingress-nginx:
  controller:
    ingressClass: nginx
    service:
      type: NodePort
  admissionWebhooks:
    enabled: false
  hostPort:
    enabled: true
    ports:
      http: 80
      https: 443
