apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guestbook-dev
  namespace: default
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: default
    name: in-cluster
  project: default
  source:
    path: apps/guestbook/dev
    repoURL: https://github.com/mhumeSF/kustomize-helm-example
    targetRevision: HEAD
    plugin:
      name: kustomize-helm
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
    - allowEmpty=true

# --- # Adding this example of app of apps. Not certain if will use
# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: apps
# spec:
#   destination:
#     namespace: argocd
#     server: in-cluster
#   project: default
#   source:
#     directory:
#       recurse: true
#       include: '**/app.yaml'
#     # path: apps/dev
#     repoURL: https://github.com/mhumeSF/kustomize-helm-example
#     targetRevision: HEAD
#     plugin:
#       name: kustomize-helm
